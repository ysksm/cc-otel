# Build output (anchored so they don't match the cmd/ccotel/ source dir)
/bin/
/ccotel
/ccotel.exe

# Local data (DuckDB file, downloaded native libs, dev key)
/data/
*.duckdb
*.duckdb.wal
dev-api-key.txt
libduckdb.so
libduckdb.dylib
duckdb.dll
*.zip

# Frontend
web/node_modules/
web/dist/

# Editor / OS
.DS_Store
*.swp

# Scratch
e.txt
err.txt

# Embedded SPA build output (generated by Vite)
internal/webui/dist/*
!internal/webui/dist/.gitkeep

# Cross-compiled binaries
/dist/
